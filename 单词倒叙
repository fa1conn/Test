#include<stdio.h>
#include<string.h>
int main()
{
    int i;  

char str[128];
gets(str);
int len = strlen(str);
for (i = len-1; i >= 0; i--) {
str[len] = 0;
if (str[i] ==' ') {
len = i;
printf("%s ", &str[len+1]);
}
}
printf("%s", &str[0]);
return 0;
}
