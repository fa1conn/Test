#include<stdio.h>
#include<string.h>
int main()
{
    int i;  

char a[100];
gets(a);
int b = strlen(a);
for (i = b-1; i >= 0; i--) {
a[b] = 0;
if (a[i] ==' ') {
b = i;
printf("%s ", &a[b+1]);
}
}
printf("%s", &a[0]);
return 0;
}

/*扔c里面警告，扔c++正常，可能是我看的c书混入了c++的语法规则，望各位师傅指教*/
